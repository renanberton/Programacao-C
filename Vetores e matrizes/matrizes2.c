#include <stdio.h>

int main()
{
    //Matriz de inteiros
    int numeros[2][2];
    numeros[0][0] = 1;
    numeros[0][1] = 2;
    numeros[1][0] = 3;
    numeros[1][1] = 4;

    for (int i = 0; i < 2; i++)
    {
        for (int j = 0; i < 2; j++)
        {
            printf("numeros[%d][%d] vale %d\n", i, , j, numeros[i][j]);
        }
    }

    //Matriz de reais
    float valores[5][5];

    return 0;
}